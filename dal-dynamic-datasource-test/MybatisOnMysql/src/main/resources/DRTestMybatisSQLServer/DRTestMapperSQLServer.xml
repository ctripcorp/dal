<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DRTestMybatisSQLServer.DRTestSQLServerMapper">
    <select id="getDRTestMybatisSQLServerPojo" resultType="DRTestMybatisSQLServer.DRTestMybatisSQLServerPojo"
            parameterType="int">
        SELECT * FROM testTable WHERE PeopleID = #{peopleID}
    </select>
    <select id="getCount" resultType="int">
        SELECT COUNT(*) FROM testTable
    </select>
    <!--<select id="findForumByName" resultType="Forum" parameterType="string">
        SELECT
        forum_id  forumId,
        forum_name  forumName,
        forum_desc  forumDesc
        FROM t_forum f
        WHERE f.forum_name LIKE #{forumName}
    </select>-->
    <!--<insert id="addDRTestMybatisSQLServerPojo" parameterType="DRTestMybatisSQLServer.DRTestMybatisSQLServerPojo">
        INSERT INTO testTable (Name,CityID)
        VALUES (#{name,jdbcType=NVARCHAR},#{cityID})
    </insert>-->
    <update id="updateDRTestMybatisSQLServerPojo" parameterType="DRTestMybatisSQLServer.DRTestMybatisSQLServerPojo">
        UPDATE testTable
        SET NAME =#{name},CityID=#{cityID}
        WHERE PeopleID = #{peopleID}
    </update>


    <select id="getHostNameSQLServer" resultType="string">
        select @@SERVERNAME
    </select>
    <delete id="truncateTableSQLServer">
        truncate table testTable
    </delete>
    <insert statementType="CALLABLE" id="addDRTestMybatisSQLServerPojo"
            parameterType="DRTestMybatisSQLServer.DRTestMybatisSQLServerPojo">
        {call spA_testtable_i (
        #{peopleID,mode=OUT,jdbcType=BIGINT},
        #{name,jdbcType=NVARCHAR},
        #{cityID,jdbcType=INTEGER},
        #{provinceID,jdbcType=INTEGER},
        #{countryID,jdbcType=INTEGER})}
</insert>
</mapper>
